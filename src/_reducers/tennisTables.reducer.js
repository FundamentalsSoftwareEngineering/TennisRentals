import {tennisTables as tennisTablesList} from "../_data/tennisTables";

const initialState =  tennisTablesList;

export function tennisTables(state = initialState, action) {
    switch (action.type) {

        default:
            return state
    }
}